out_freq_band: { choices: [ delta, theta, alpha, beta, gamma ] }
in_freq_band: { choices: [ all ] }  # if 'same', it will use the same as out_freq_band

# -----------------
# The ML model operating on the biomarkers
# -----------------
MLModel:
  model: LinearRegression
  kwargs: {}